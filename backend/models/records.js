const Validator = require('fastest-validator');
const recordSchema = {

    
}

 const recordValidator = new Validator();

 module.exports.recordSchema = recordSchema;
 module.exports.recordValidator = recordValidator;